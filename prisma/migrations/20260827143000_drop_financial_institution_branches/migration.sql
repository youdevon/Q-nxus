-- Unused scaffold: branch directory was never wired. Branch identity lives on
-- EmployeeBankAccount / payment snapshots (branchCode, branchName).
DROP TABLE IF EXISTS "payroll"."financial_institution_branches";
